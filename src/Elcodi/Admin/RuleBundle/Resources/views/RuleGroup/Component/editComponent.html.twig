{% extends 'AdminCoreBundle::Common/formComponent.html.twig' %}

{% form_theme form 'AdminCoreBundle::Forms/fields.html.twig' %}

{% block title %}
    {{ 'Edit RuleGroup'|trans }} - {{ entity.id }}
{% endblock title %}

{% block content %}

    {{ form_start(form, {'action': url('admin_rule_group_update', {
        'id': entity.id
    })}) }}

        {{ form_row(form.name) }}
        {{ form_row(form.code) }}
        {{ form_row(form.enabled) }}
        {{ form_row(form.rules) }}

        <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
                <button type="submit" class="button-primary button-fat pv-s mr-l">{{ 'Update'|trans }}</button>
                <a href="{{ path('admin_rule_group_list') }}" d>{{ 'Cancel'|trans }}</a>
            </div>
        </div>

    {{ form_end(form) }}

{% endblock content %}
