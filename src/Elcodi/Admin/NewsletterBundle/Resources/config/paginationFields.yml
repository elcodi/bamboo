parameters:

    #
    # Pagination fields
    #
    elcodi.admin.pagination.newsletter_subscription.fields:
        - id
        - email
        - hash
        - reason
        - createdAt
        - updatedAt
        - enabled
