<td class="ta-c">
    {% if delete is not defined or delete != false %}
        <a class="icon-trash-o c-ko td-n fz-l" title="{{ 'delete'|trans }}" href="{{ url("admin_" ~ class ~ "_delete", {"id": entity.id}) }}" data-tc-modules="confirm" data-tc-name="{{ entity.name }}" data-tc-text="{{ "Are you sure you want to delete it?"|trans }}"></a>
    {% endif %}
</td>
