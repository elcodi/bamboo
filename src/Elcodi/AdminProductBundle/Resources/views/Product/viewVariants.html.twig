{% extends '@AdminProduct/Product/view.html.twig' %}

{% block content %}

    <div class="row">
        <div class="col-md-6">
            {{ render(url("admin_product_view_component", {
            'id': id,
            })) }}
        </div>
        <div class="col-md-6">
            {{ render(url("admin_variant_list_component", {
            'id': id
            })) }}
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            {{ render(url("admin_product_gallery_component", {
            'id': id,
            })) }}
        </div>
    </div>

{% endblock content %}
