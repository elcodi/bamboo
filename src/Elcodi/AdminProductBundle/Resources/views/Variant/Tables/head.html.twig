{% extends '@AdminCore/Tables/head.html.twig' %}

{#
    This template needs this formatted data:
    {
        rows: {
            "id": {width:"25", sorting_disabled: true},
            "desc": {},
            "field": {},
        },
        class: "rule",
    }
    You can override the row name by defining "row" field
#}

{% block column_header %}
    <th class="{{ directionclass }} {% if info.width is defined %} w-{{ info.width }}"{% endif %}">
        {{ row|trans }}
    </th>
{% endblock %}