{% extends 'AdminCoreBundle::layout.html.twig' %}

{% block content_title %}
    {{ 'CurrencyExchangeRate Zone'|trans }}
{% endblock content_title %}

{% block content %}

    <div class="row">
        <div class="col-md-6">
            {{ render(url("admin_currency_exchange_rate_view_component", {
                'id': id,
            })) }}
        </div>
        <div class="col-md-6">
            {{ render(url("admin_currency_exchange_rate_new_component")) }}
        </div>
    </div>

{% endblock content %}
