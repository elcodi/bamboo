{% extends 'AdminCoreBundle::layout.html.twig' %}

{% block content_title %}
    {{ 'Currency Zone'|trans }}
{% endblock content_title %}

{% block content %}

    <div class="row">
        <div class="col-md-6">
            {{ render(url("admin_currency_view_component", {
                'id': id,
            })) }}
        </div>
        <div class="col-md-6">
            {{ render(url("admin_currency_new_component")) }}
        </div>
    </div>

{% endblock content %}
